# Gold — Tislelizumab IEGP (this source only)

Eval gold for **`BeOne_Tislelizumab IEGP_VShare 3.0.pptx`** only. Do not merge with `beone-bgb-58067-prmt5i`.

Gap IDs are **narrative** (“Need for …”) grouped by **chapter** (Transversal, Upper GI, ESCC, GC/GEJ, Lung, perioperative NSCLC). Populate `gaps[]` with `{ chapter, statement, priority_band, markets[], status }` from prioritization slides.

<<<<<<< HEAD
Tactics use **`G:{n}`** identifiers (HEOR publication tracker) plus trial references (e.g. RN-305 post-hoc). Populate `tactics[]` separately from gaps — same deck, distinct objects. Mark `origin`: **`inventory`** when tied to publication tracker / ongoing plans; **`ideated`** when created for high-priority gaps only (often not duplicated elsewhere in source).
=======
Tactics use **`G:{n}`** identifiers (HEOR publication tracker) plus trial references (e.g. RN-305 post-hoc). Populate `tactics[]` separately from gaps — same deck, distinct objects.
>>>>>>> origin/main
